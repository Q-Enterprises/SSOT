{
  "name": "google-fonts-proxy",
  "version": "1.0.0",
  "description": "Google Fonts API proxy with caching and rate limiting",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "echo \"No tests specified\" && exit 0",
    "build": "echo \"No build step required\""
  },
  "keywords": ["fonts", "proxy", "google-fonts", "cache"],
  "author": "Q-Enterprises",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "ajv": "^8.12.0"
  }
}
